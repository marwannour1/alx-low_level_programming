#include "main.h"

/**
  * print_numbers - checks if letter is upper
  *
  * Return: 1 if upper 0 if lower
  *
  */
void print_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
		_putchar('0' + i);
	_putchar('\n');
}
